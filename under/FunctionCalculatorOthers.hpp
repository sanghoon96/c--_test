#include "FunctionCalculatorOthers.cpp"

int add(int first, int second);
int sub(int first, int second);
int mul(int first, int second);
int divi(int first, int second);
int mod(int first, int second);